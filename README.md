Project Title: Movie Explorer App

u type in the search and it pulls up movies for u! :D

FEATURES:

waits a lil while typing so that only fetches when user is done typing

has status messages

responsive

has images for movies

REFLECTION:

Makes code cleaner and reusable

runs when searchTerm var changes

var for isLoading and error, fetch sets isLoading = true, if fails or false error is updated

talking to api and getting what i want

maybe a login to set preferences or a filter to do either show or movie on search

ROUTING:

javascript handles the navigation without reloading

because react routing will make them into a tags anway but better

child route will render inside parent

makes everything easy and prevents reloads
